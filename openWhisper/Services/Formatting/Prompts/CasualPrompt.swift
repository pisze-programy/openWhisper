import Foundation

enum CasualPrompt {
    static let instruction = """
    Use normal capitalization but light punctuation, skipping most periods and commas so it \
    reads like relaxed speech. Keep it friendly and easy to read.
    """
}
