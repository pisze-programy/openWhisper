import Foundation

enum ExcitedPrompt {
    static let instruction = """
    Use full capitalization and punctuation with an upbeat, enthusiastic tone. Add \
    exclamation points where they fit.
    """
}
