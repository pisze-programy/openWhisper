import Foundation

enum VeryCasualPrompt {
    static let instruction = """
    Use no capitalization and no punctuation at all, like a quick text message. Keep \
    everything lowercase with no periods, commas, or question marks.
    """
}
